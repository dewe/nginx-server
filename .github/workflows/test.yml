name: Test

on: [push, pull_request]

jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - run: docker build -f docker/Dockerfile.test -t nginx-server-test .
            - run: docker run --rm nginx-server-test
